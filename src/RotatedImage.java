import javafx.scene.image.Image;

public class RotatedImage extends Image {
    public RotatedImage(String url) {
        super(url, true);
    }
    public int getRotation() {
        
    }
}